
version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      # Mounts the project directory into the container for development.
      # For production, you might want to remove this or make it read-only.
      - .:/app
      # Exclude node_modules from being overwritten by the host's node_modules
      - /app/node_modules
      # Exclude the dist folder from being overwritten if you build outside and copy
      - /app/dist
    environment:
      # Supabase connection settings
      VITE_SUPABASE_URL: "https://regozquxnqhcnziyyuvv.supabase.co"
      VITE_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJlZ296cXV4bnFoY256aXl5dXZ2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDcyNzUzMDksImV4cCI6MjA2Mjg1MTMwOX0.-BRPhPctZhiKO-vXdDAHFJkBcJANjc3c78iYmQVADVs"
      # Application settings
      HOST: "0.0.0.0"
      PORT: "8080"
